# Contributing

1. Create a focused branch.
2. Keep infrastructure changes separate from feature changes.
3. Add tests for changed behavior.
4. Run `make quality` before opening a pull request.
5. Explain migrations, compatibility risks and rollback where relevant.

Never commit `.env`, credentials, generated secrets, `vendor` or `node_modules`.
