# Security

Do not report vulnerabilities through public issues. Use the repository owner's private security contact.

Never commit secrets. Local tools such as phpMyAdmin and Mailpit are development-only and must not be exposed publicly or reused as a production deployment design.
