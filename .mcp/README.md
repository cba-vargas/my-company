# MCP Configuration

`mcp.json.example` is a client-neutral starting example using the common `mcpServers` shape. Individual clients can use a different file location or schema; consult that client's current documentation before enabling it.

Do not commit `.mcp.json` or any secret. Keep filesystem scope limited to this repository.
